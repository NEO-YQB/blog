    <div class="co-md-12 co-lg-12 mb-30">
        <div class="box">
            <div class="box-head">
                <h4 class="title"> ویرایش مطلب </h4>
            </div>
            <div class="box-body">
                <div class="row mbn-20">

                    {{!-- form field --}}
                    <form  action="/admin/posts/update/{{post.ID}}" method="post">
                        <div class="col-lg-20 col-12 mb-20">
                        {{#if hasError}}
                        <div class="alert alert-outline-secondary" role="alert">
                            {{#each errors}}
                            <span>{{this}}</span>
                            {{/each}}                            
                        </div>
                        {{/if}}
                        <div class="col-12 mb-30">
                            <input type="text" name="title" placeholder="عنوان مطلب" id="" class="form-control" value="{{post.title}}">
                        </div>
                        <div class="col-12 mb-30">
                            <input type="text" name="slug" placeholder="نامک مطلب" id="" class="form-control" value="{{post.slug}}">
                        </div>
                        <div class="col-12 mb-30">
                            <textarea class="form-control summernote" name="content" placeholder="محتوا مطلب" id="" >{{post.content}}</textarea>
                        </div>
                        
                        <div class="col-12 mb-30">
                            <h6 class="mb-15">وضعیت مطلب</h6>
                            
                            <label for=""><input type="radio" name="status" id="" value="{{postStatus.DRAFT}}" {{#isSelectedStatus postStatus.DRAFT}} checked {{/isSelectedStatus}}>پیش نویس</label>
                            <label for=""><input type="radio" name="status" id="" value="{{postStatus.REVIEW}}" {{#isSelectedStatus postStatus.REVIEW}} checked {{/isSelectedStatus}}>در حال بازبینی</label>
                            <label for=""><input type="radio" name="status" id="" value="{{postStatus.PUBLISHED}}" {{#isSelectedStatus postStatus.PUBLISHED}} checked {{/isSelectedStatus}}>منتشر شده</label>

                        </div>
                        <div class="col-12 mb15">
                            <select name="author" class="form-control" id="">
                                {{#each users}}
                                <option value="{{this.id}}" {{#isPostAuthor this.id}}selected{{/isPostAuthor}}>{{this.full_name}}</option>
                                {{/each}}
                            </select>
                        </div>
                        <br>
                        <div class="col-12 mb-30">
                            <button type="submit" class="button button-primary"><span>ذخیره سازی</span></button>
                        </div>
                    </div>
                    </form>

                </div>
            </div>
        </div>
    </div>